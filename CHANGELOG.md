# سجل الإصدارات

## 1.17.4 — 2026-08-26

- تحسين داخلي في طريقة كتابة سجل الإصدارات — من غير أي تغيير في النظام نفسه.

## 1.17.3 — 2026-08-26

- **توحيد الأرقام في كل النظام**. الأرقام كانت بتظهر بالعربي (١٩٠٠٫٠٠) في الجداول وبالإنجليزي (19) في خانات الإدخال، لأن خانة الرقم في أي متصفح بتكتب إنجليزي مهما كانت اللغة. دلوقتي كله رقم واحد موحّد على الشاشة وفي التصدير وفي الطباعة.
- **إصلاح: الكلام العربي كان بيتلزق في صورة التقرير** ("اسم الصنف عند المورد" كانت بتطلع "اسمالصنفعندالمورد"). المكتبة الخارجية اللي كانت بترسم الصورة بتعيد ترتيب الكلمات بنفسها وبتضيّع المسافات في العربي. بقى الرسم مباشر على `canvas` فالمتصفح هو اللي بيتعامل مع النص — وكمان تصدير الصور بقى شغال من غير إنترنت.
- **إصلاح: الطباعة الحرارية من الموبايل**. كانت بتفتح نافذة منبثقة، ومتصفحات الموبايل بتمنعها افتراضيًا، فكنت تشوف "المتصفح منع فتح نافذة الطباعة" كل مرة. بقت بتطبع من غير نافذة منبثقة خالص.
- **قائمة البحث عن صنف ERP بقت أوسع**. كانت بتتقص بعد صف واحد تقريبًا على الموبايل، دلوقتي بتعرض ٤-٦ أصناف وتنزل بالسحب.

## 1.17.2 — 2026-08-26

- **اختبارات تلقائية قبل أي نشر**. أي رفعة بتشغّل ٣٠ فحص في متصفح حقيقي على كل الشاشات، **ولو وقعت، رقم الإصدار مش بيترفع** — يعني النسخة اللي فيها مشكلة عمرها ما تتبعت تلقائيًا للأجهزة اللي فاتحة النظام.

## 1.17.1 — 2026-08-25

- تحديث تلقائي.

## 1.17.0 — 2026-08-25

**فحص شامل للنظام: إصلاح مشاكل في السلوك، وتسريع كبير في فتح الشاشات والحفظ والتعديل.**

### أسرع بشكل ملحوظ

- **فتح المرتجعة بقى أسرع بكتير**. النظام كان بيراجع ربط كل صنف بالـ ERP صنف ورا صنف، يعني مرتجعة فيها ٢٥ صنف كانت بتعمل **٢٨ رحلة للسحابة** قبل ما الشاشة تظهر. دلوقتي بقت **٤ رحلات مهما كان عدد الأصناف**.
- **الكتابة في خانات البحث بقت فورية**. كل حرف تكتبه في بحث المرتجعات أو الأصناف كان بينزّل قواعد البيانات من السحابة من الأول. دلوقتي في ذاكرة مؤقتة قصيرة (١٥ ثانية) بتتمسح فورًا مع أي تعديل، فالبحث بقى **بصفر رحلات** والبيانات تفضل محدّثة زي ما هي.
- **التنقل بين الحقول من غير تعديل مبقاش بيكتب حاجة**. لو مشيت على خانات الكمية والتكلفة في مرتجعة عشان تراجعها بس، النظام كان بيحفظ كل خانة لمست فيها — ٨ خانات كانت بتساوي **٢١ عملية كتابة** على السحابة من غير ما تغيّر رقم واحد. دلوقتي **صفر** (والتعديل الحقيقي لسه بيتحفظ عادي).
- **صفحة المورد** كانت بتقرا نفس البيانات مرتين — اتقلّت من ١٠ رحلات لـ ٦.
- **قائمة المرتجعات بقى فيها ترقيم صفحات فعلي**. الكود كان أصلًا معمول عشان يقسّم على صفحات بس مكانش بيستخدمه، فكانت بتعرض كل المرتجعات من أول واحدة في صفحة واحدة. نفس المشكلة كانت في شاشة مراجعة الفواتير — اتصلحت في الاتنين.
- **شاشة "أصناف الموردين"** بقت تعرض أول ٥٠ صنف لكل مورد وزرار "عرض باقي الأصناف" — بدل ما ترسم آلاف الصفوف مرة واحدة.
- **الصفحة الرئيسية** بقت تحدّث نفسها كل دقيقة بدل كل ٣٠ ثانية، وبتوقف تمامًا لو التبويب مش ظاهر قدامك.

### إصلاح مشاكل

- **البحث كان بيفقد التركيز مع كل حرف**. في كل شاشات البحث (المرتجعات، الأصناف، الموردين، سجل العمليات، مراجعة الفواتير) الشاشة كانت بتترسم من الأول بعد كل حرف فالمؤشر كان بيطلع من الخانة. دلوقتي التركيز ومكان المؤشر بيرجعوا مكانهم بالظبط.
- **قوائم الاقتراحات كانت بتفضل مفتوحة**. الضغط بره القائمة كان المفروض يقفلها، بس الكود كان بيسجّل المستمع بـ "مرة واحدة بس" — فأول ضغطة في أي مكان (حتى جوه القائمة نفسها) كانت بتستهلكه، وبعدها القائمة عمرها ما تتقفل تاني.
- **"إضافة مورد جديد" من شاشة مراجعة الفواتير مكانش بيربط المورد**. الشاشة كانت مستنية بيانات المورد اللي اتحفظ، بس ما كانتش بتوصلها أبدًا — فكنت تضيف المورد وتلاقيه مش متحدد.
- **الضغط مرتين بسرعة على "مرتجعة جديدة" (أو "مراجعة جديدة") كان ممكن يحرق رقمين**. الزرار بقى بيتقفل لحظة الضغط.
- **"الكمية الفعلية: X قطعة" في مراجعة الفواتير** كانت بتفقد كلمة "قطعة" أول ما تعدّل الصف.
- **حفظ نفس التكلفة تاني مكانش المفروض يتسجّل كتغيير سعر** — كان بيضيف سطر جديد في سجل التكلفة بنفس الرقم كل مرة.
- **الأزرار اللي كانت بتفشل في صمت بقت تقولك السبب**. لو سجل اتحذف من جهاز تاني وانت فاتح الشاشة القديمة، الضغط على أي زرار كان بيعمل خطأ في الخلفية والشاشة ما بتردش. دلوقتي بتظهر رسالة واضحة.
- **خانة البحث في نافذة الربط** كانت بتعرض ٨ أصناف عشوائية لما تفضيها بدل ما تخفي القائمة.
- **ملف Excel من غير أوراق بيانات** كان بيوقف المعالج بخطأ بدل رسالة مفهومة.
- **"تم استلام كل البدائل"** بقى عملية كتابة واحدة مجمّعة بدل عملية لكل صنف.

### تنظيف داخلي

- ملف `.github/workflows/bump-version-and-deploy.yml` (اللي التوثيق بيتكلم عنه من الإصدار 1.1.0) **كان ناقص من المشروع أصلًا** — يعني رفع رقم الإصدار التلقائي مع كل رفعة مكانش شغال فعليًا. اتضاف دلوقتي.
- اتشال ملف `js/core/brand.js` — كان بيكتب نص ثابت موجود أصلًا في الصفحة، من غير أي تأثير.
- اتشالت استيرادات ميتة من ٧ ملفات، واتصلحت تعليقات قديمة بتقول إن المزامنة السحابية "لسه مش موجودة".

## 1.16.1 — 2026-08-23

- **إصلاح: التحديد (Ctrl+A) بقى ظاهر فعليًا**. لون التحديد كان فاتح جدًا وبيتشيل تقريبًا داخل الحقول البيضاء. اتغيّر للون أغمق وأوضح وبقى شغال جوه كل الحقول والخانات.
- **إضافة صنف في المرتجعة**: اختيار صنف من الاقتراحات بقى ياخدك على خانة الكمية مباشرة (مش التكلفة)، والكمية بقت فاضية بدل ما تبدأ بـ 1 تلقائيًا — لازم تكتبها إنت بنفسك في كل مرة، أأمن من إنك تنسى تغيّرها وتضيف كمية غلط بالغلط.

## 1.16.0 — 2026-08-16

- **استبدال بضاعة سليمة — نوع معالجة جديد لكل صنف داخل المرتجعة**: كل صنف في المرتجعة بقى ليه "نوع معالجة" مستقل — **دائن** (زي ما كان، محتاج تسجيل على ERP) أو **استبدال** (المورد بيبدله ببضاعة سليمة، ومحتاجش يتسجل على ERP خالص). تقدر تخلط الاتنين في نفس المرتجعة عادي (بعض الأصناف دائن وبعضها استبدال).
- لكل صنف "استبدال"، تتبّع حالة استلام البديل (⏳ لسه / ✅ اتستلم) — تقدر تسجّلها صنف بصنف، أو بزرار واحد "تم استلام كل البدائل" لو استلمت المرتجعة كلها مرة واحدة.
- شارة ERP في قائمة المرتجعات والصفحة الرئيسية بقت أدق: لو المرتجعة كلها استبدال (مفيهاش أي صنف دائن)، مش هتفضل شايلة "🔴 غير مسجلة" اللي كانت بتوهم إنها متأخرة رغم إنها أصلًا مش هتتسجل أبدًا.

## 1.15.1 — 2026-08-15

- **إصلاح عاجل**: شاشة "أصناف الموردين" كانت بتوقف بخطأ (`fmtInt is not defined`) بسبب استيراد ناقص في الكود — كانت النسخة اللي بعتهالك فيها الخطأ ده من الأول، آسف عليه. اتصلح دلوقتي وراجعت باقي الملفات للتأكد إن مفيش استيراد ناقص تاني في أي مكان.
- **ربط صنف بـ ERP من جوه المرتجعة مباشرة**: أي صنف "⚠️ غير مرتبط" في جدول أصناف المرتجعة بقى جنبه زرار "ربط" يفتحلك نافذة الربط على طول، من غير ما تحتاج تسيب المرتجعة وتروح لشاشة أصناف المورد.
- **زرار "+ إضافة صنف" جنب كل مورد** في شاشة "أصناف الموردين" — تضيف صنف لمورد معيّن من غير ما تدخل على صفحته.

## 1.15.0 — 2026-08-15

- **شاشة جديدة "أصناف الموردين"** (تحت قسم "الموردون"): كل الموردين وأصنافهم في شاشة واحدة، اسم كل مورد وتحته أصنافه مباشرة، بنفس خصائص الربط والتكلفة والحذف، وبحث عام بيدوّر في كل الموردين والأصناف مع بعض مش مورد واحد بس.
- **زراير حذف بقت متاحة من برة**: قائمة الموردين وقائمة أصناف ERP بقى فيهم زرار "حذف" مباشر في كل صف، من غير ما تحتاج تدخل على المورد أو تفتح نافذة تعديل الصنف الأول. نفس فحوصات الأمان اللي كانت موجودة (منع حذف مورد له مرتجعات، تحذير عند حذف صنف مرتبط بموردين) لسه شغالة بالظبط زي ما هي.

## 1.14.0 — 2026-08-14

- **حذف مورد**: زرار "حذف المورد" في صفحة المورد. لو له أي مرتجعات (نشطة أو مؤرشفة) النظام بيرفض الحذف ويقولّك تتصرف فيها الأول — عشان مينفعش تفقد اسم المورد من مرتجعات موجودة فعلًا. لو مفيهوش مرتجعات، بيتحذف مع كل أصنافه المرتبطة بيه.
- **حذف صنف ERP**: زرار "حذف الصنف" في نافذة تعديل الصنف. لو مرتبط بموردين، بيحذّرك بالعدد وبيفك الربط تلقائيًا (بترجع "غير مرتبطة" بدل ما تفضل معلّقة على صنف محذوف). المرتجعات السابقة اللي استخدمت الصنف ده مش بتتأثر.
- **حذف صنف عند مورد**: زرار "حذف" جديد في جدول أصناف المورد (كانت الدالة موجودة من قبل بس مش متوصّلة بأي زرار).

## 1.13.0 — 2026-08-13

- **إصلاح: الكمية الفعلية بالقطعة كانت بتختفي من التصدير**. الأداة الأصلية كانت أصلًا بتظهرها في النسخ، وأنا وقت النقل نسيتها من التقارير والصورة والطباعة — رجّعتها في كل مكان (الجدول، النسخ، الصورة، الطباعة)، فلو كتبت الكمية بالدستة، هتلاقي "الكمية الفعلية: X قطعة" ظاهرة في كل تصدير مش بس على الشاشة.
- **ربط أصناف مراجعة الفواتير بقاعدة أصناف ERP**: خانة "الصنف" في إضافة صنف جديد بقت بتقترح أصناف موجودة في النظام وانت بتكتب، ولو اخترت واحد بيتربط بيه مباشرة. لو الصنف مش موجود أو مش عايز تربطه، تقدر تكتب اسمه عادي وهيتحفظ بعلامة "⚠️ غير مرتبط".

## 1.12.0 — 2026-08-12

- **حقل "ق / د" في إضافة صنف بالمرتجعة**: خانة صغيرة قبل التكلفة — "ق" (الافتراضي) يعني السعر بالقطعة، و"د" يعني السعر بالدستة، وساعتها النظام بيقسمها على 12 تلقائيًا ويحسب سعر القطعة قبل ما يضربها في الكمية. اختيار صنف من الاقتراحات بيرجّع الخانة لـ"ق" تلقائيًا (لأن التكلفة المقترحة دايمًا بسعر القطعة).
- **إصلاح: زرار "+ إضافة" في المرتجعة بقى بيتقفل أثناء التنفيذ** — الضغط عليه مرتين بسرعة (مثلًا وهو مستني الرفع للسحابة) كان ممكن يضيف نفس الصنف مرتين؛ دلوقتي الزرار بيتعطل لحظة الضغط ويوريك "جارِ الإضافة..." لحد ما يخلص.

## 1.11.0 — 2026-08-11

- **مراجعة الفواتير — أداة جديدة مستقلة تمامًا** (قسم "أدوات مساعدة" في الشريط الجانبي)، منقولة من الأداة الجانبية اللي كانت موجودة، بألوان وخط النظام:
  - إضافة بنود (كمية × سعر) مع وحدات قابلة للتعديل بالكامل (قطعة، دستة، أو أي وحدة تضيفها بمضاعفها الخاص زي كرتونة أو نص دستة).
  - قائمة كاملة لكل المراجعات السابقة، قابلة للرجوع والتعديل في أي وقت — بعد ما كانت الأداة الأصلية بتفقد كل حاجة عند تحديث الصفحة.
  - رقم مراجعة تلقائي، رقم فاتورة، تصوير الفاتورة وحفظها مع المراجعة، ولمبة 🔴/🟢 لحالة "تم الإدخال على ERP".
  - ربط بمورد موجود في النظام، أو إضافته كمورد جديد من نفس الشاشة.
  - نسخ كنص، تنزيل كصورة، مشاركة واتساب، وطباعة — ومحول الأرقام إلى كلمات عربية كأداة سريعة منفصلة.
  - بيانات مستقلة تمامًا عن المرتجعات والأصناف — مفيش أي تأثير على أي إحصائية أو تقرير موجود.
- **تحسين شاشة "إضافة اسم صنف عند المورد"**: بقت بتقترح أصناف مشابهة موجودة بالفعل أثناء الكتابة (لتفادي التكرار)، وبقى فيها خانة ربط بصنف ERP وخانة تكلفة، فتقدر تدخل بيانات الصنف كاملة من نفس الشاشة بدل ما تحتاج تفتح 3 نوافذ منفصلة.

## 1.10.1 — 2026-08-10

- إصلاح بنيوي (Circular Import): `items.js` و`supplier-items.js` كانا بيستوردوا من بعض بسبب دالتين مشتركتين. فصلتهم في ملف جديد `modules/item-links.js`، فدلوقتي كل واحد فيهم بيعتمد على الملف الجديد بس، من غير ما يحتاجوا بعض. مفيش أي تغيير في سلوك النظام — إصلاح بنية داخلية بحت.
- شيلت مجلدات فاضية باقية غلط من إعداد المشروع الأول (اسمها كانت بادئة بـ `{`) — دي كانت فاضية تمامًا فمكانتش بتتحفظ في Git أصلًا، فمفيش حاجة تتمسح يدويًا لو رفعتها قبل كده.

## 1.10.0 — 2026-08-09

- زرار "📱 مشاركة عبر واتساب" جديد في خيارات تصدير التقرير، بنفس الأعمدة القابلة للإخفاء زي باقي طرق التصدير. بيولّد نفس صورة التقرير ويفتح قائمة المشاركة في الموبايل عشان تختار واتساب والشخص مباشرة (الصورة بتبقى مرفقة جاهزة). على الأجهزة اللي مبتدعمش مشاركة الملفات (غالبًا الكمبيوتر)، بينزّل الصورة ويفتح واتساب مع تذكير ترفقها يدويًا.

## 1.9.0 — 2026-08-09

- **إصلاح فعلي**: صنف المورد كان بيتكرر لو الاسم مكتوب بفروق بسيطة (مسافة زيادة، "كريبسادة" بدل "كريب سادة"، اختلاف في الألف أو التاء المربوطة...) بدل ما يتجمع على نفس السجل. دلوقتي المطابقة بتتجاهل الفروق دي (زي البحث بالظبط)، فنفس الصنف لنفس المورد بيتجمع صح. أصناف ERP لسه ممكن تتكرر بالاسم عادي زي ما هو متوقع — ده مقصود ومختلف عن أصناف الموردين.
- **تحسين أداء كبير في المرتجعات**: تعديل الكمية أو التكلفة كان بيعمل حتى 7-8 عمليات قراءة/كتابة متتالية على السحابة لكل تعديل واحد (خصوصًا بعد التحول لـ Firebase)، وده اللي كان بيحس إنه بطيء وقت الكتابة. دلوقتي قللتها لعملية أو اتنين بس في المسار الحرج، والباقي (سجل العمليات، تحديث "آخر تعديل" على المرتجعة) بيحصل في الخلفية من غير ما يوقفك.
- **فلاتر في صفحة المورد**: "غير مرتبط فقط" و"بدون تكلفة فقط" على قائمة أصناف المورد، بنفس شكل الفلاتر في باقي الشاشات.
- **الصفحة الرئيسية بقت تتحدّث تلقائيًا**: كل 30 ثانية وهي مفتوحة، وكل مرة ترجع لتبويبها بعد ما تكون بعيد عنه — بدل ما تفضل واقفة على نفس الأرقام لحد ما تتنقل بره وترجع تاني.

## 1.8.0 — 2026-08-09

- **زرار رجوع** في أعلى الشاشة جنب اسمها، بيظهر في كل الشاشات ما عدا الرئيسية — مهم خصوصًا لو النظام مُثبَّت كتطبيق مستقل على الموبايل (مفيش شريط متصفح فيه زرار رجوع في الوضع ده).
- **فلاتر جديدة** بشكل موحّد في كل الشاشات:
  - سجل العمليات: فلتر بالتاريخ (من - إلى) وفلتر بنوع العملية (مورد / صنف ERP / صنف مورد / مرتجعة / نظام).
  - المرتجعات: فلتر بالتاريخ (من - إلى) بجانب فلتر المورد الموجود.
  - قاعدة أصناف ERP: فلتر "مرتبط بموردين / غير مرتبط بأي مورد" بجانب فلتر القسم الموجود.
  - الأصناف غير المرتبطة: فلتر بالمورد.
  - زرار "✕ مسح الفلاتر" بيظهر تلقائيًا في أي شاشة فيها فلتر مُفعّل.

## 1.7.0 — 2026-08-09

- **إصلاح شامل لتوافق الجداول مع شاشات الموبايل الصغيرة**: كل جداول النظام (المرتجعات، أصناف المرتجعة، الموردين، أصناف ERP، أصناف الموردين، سجل التكلفة، معالج استيراد Excel) بقت تتحول تلقائيًا لكروت مرتبة فوق بعضها على الشاشات الضيقة بدل جدول عريض بيتقطع أو محتاج تمرير أفقي.
- الإيصال الحراري: اسم صنف ERP تحت اسم المورد بقى أوضح (لون أسود غامق بدل رمادي فاتح)، النقط الفاصلة بين كل صنف والتاني بقت خط متقطع أسود واضح بدل نقط رمادية باهتة، وجملة "نظام إدارة المخزون والمرتجعات" بقت في سطر واحد بدل ما تتقطع لسطرين.
- الشريط الجانبي رجع يعرض "مرتجعات الموردين" ثابتة زي الأول — اسم المحل بقى مستخدم بس في التقارير المُصدَّرة (Excel، الصورة، الإيصال الحراري)، مش في واجهة النظام نفسها.

## 1.6.0 — 2026-08-09

- تمييز "تكلفة النظام الافتراضية" باللون الأحمر: لو مورد لسه محدّدتش له تكلفة صنف معيّن، وده مرتبط بصنف ERP، بدل ما تظهر التكلفة صفر بيتم اقتراح تكلفة النظام (baseCost) تلقائيًا كـ نقطة بداية — وتظهر باللون الأحمر في شاشة المرتجعة (في الاقتراحات وفي جدول الأصناف) عشان تعرف إنها مش تكلفة المورد الفعلية المؤكدة. التكلفة دي **مش** بتتسجل كتكلفة رسمية للمورد ولا بتتحفظ في سجل تاريخ التكلفة إلا لو عدّلتها إنت بنفسك (وقتها بترجع تتلون عادي وتتسجل كتكلفة مؤكدة).

## 1.5.1 — 2026-08-09

- إصلاح: خانة "صنف النظام ERP" في خيارات التصدير كانت شغالة في Excel والصورة بس، ومالهاش أي تأثير على الإيصال الحراري. دلوقتي لو علّمتها، اسم صنف ERP بيظهر كسطر صغير تحت اسم صنف المورد في الإيصال، ولو شِلت علامة "اسم الصنف عند المورد" واسيبت "صنف النظام ERP" بس، اسم ERP بيبقى هو العنوان الأساسي بدل اسم المورد.

## 1.5.0 — 2026-08-09

- **مزامنة سحابية حقيقية عبر Firebase (Firestore)**: النظام بقى بيستخدم قاعدة بيانات سحابية بدل التخزين المحلي فقط، فأي جهاز يفتح نفس الرابط بيشوف نفس البيانات (الموردين، الأصناف، المرتجعات، سجل العمليات — كل حاجة).
- تسجيل دخول تلقائي مجهول (Anonymous Auth) بدون أي شاشة تسجيل تظهر للمستخدم — وده اللي بيسمح بتفعيل قواعد أمان تمنع أي حد بره النظام من الدخول.
- نقل تلقائي (مرة واحدة، آمن) لأي بيانات محلية موجودة على الجهاز من قبل التحديث إلى السحابة أول مرة يفتح فيها النظام بعد التحديث — بدون حذف أي حاجة.
- النظام لسه بيشتغل أوفلاين (عرض/تعديل)، ماعدا إنشاء مرتجعة جديدة برقم جديد اللي محتاج اتصال إنترنت لحظة الإنشاء.
- نقطة حالة الاتصال في الشريط الجانبي بقت شغالة فعليًا: 🟡 بتضيء أثناء أي عملية حفظ سحابية حقيقية.
- ملف `firestore.rules` جاهز يُلصق في Firebase Console لتفعيل قواعد الأمان.
- تعديلات على الإيصال الحراري: شعار المحل + اسم النظام دايمًا ظاهرين ناحية اليمين (حتى لو اسم المحل فاضي)، اسم منشئ النظام ناحية الشمال، إزالة تكرار اسم المحل من أسفل الورقة، وتقليل المسافات بين الأسطر أكتر لتقصير طول الورقة.

## 1.4.1 — 2026-08-08

- نقطة حالة اتصال في الشريط الجانبي: 🟢 متصل بالإنترنت، 🟡 جارِ رفع البيانات، 🔴 غير متصل (التعديلات بتتحفظ محليًا لحد ما النت يرجع). دلوقتي بتعكس الاتصال الفعلي بالإنترنت، وهتشتغل تلقائيًا مع حالة "جارِ الرفع" بمجرد ما مزامنة Firebase تتفعّل.

## 1.4.0 — 2026-08-08

- خط النظام بقى أوضح: وزن الخط الأساسي في كل الشاشات بقى أثقل شوية (Medium بدل Regular)، والأرقام في الجداول والتقارير بقت بخط أغمق (Bold) للوضوح.
- إيصال الطباعة الحرارية: السعر والإجمالي بقوا في نفس السطر بدل سطرين منفصلين، والمسافات بين الأسطر بقت أضيق فطول الورقة أقصر، والأرقام كلها بقت غامقة (Bold).
- إضافة "إجمالي الكمية" في كل أنواع التصدير (Excel، الصورة، الإيصال الحراري) بجانب إجمالي القيمة.

## 1.3.0 — 2026-08-08

- شاشة "الإعدادات" جديدة: اسم المحل (قابل للتعديل في أي وقت، بحفظ تلقائي)، ويظهر في الشريط الجانبي وفي كل التقارير المُصدَّرة.
- نسخة احتياطية كاملة لكل بيانات النظام (تنزيل ملف JSON واحد) واستعادتها على أي جهاز — لنقل البيانات يدويًا بين الأجهزة لحد ما تُفعَّل المزامنة السحابية.
- إصلاح طباعة الإيصال الحراري: تصميم أبسط بدون Flexbox أو هوامش مزدوجة، عرض مرن بدل عرض ثابت بالمليمتر، مع التفاف النص بدل قصّه — لحل مشكلة اختفاء/عدم وضوح بعض الكلام عند الطباعة.
- إضافة خانة "التكلفة" في صف إضافة صنف بشاشة المرتجعة، فتقدر تدخل الاسم والكمية والتكلفة مرة واحدة.
- أي تكلفة تتغيّر من جوه مرتجعة (سواء وقت الإضافة أو بعدين) بقت تتحدّث تلقائيًا في "أصناف المورد" برا (مع سجل تاريخي)، من غير ما تغيّر تكلفة أي مرتجعة سابقة محفوظة.
- ربط الصنف بـ ERP بقى يتحدّث تلقائيًا داخل المرتجعات المفتوحة فور ما تربطه من شاشة أصناف المورد، من غير ما تحتاج تعمل حاجة يدوي.
- ترتيب أصناف المرتجعة بقى حسب وقت الإضافة (آخر صنف تضيفه يظهر في الآخر)، مش ترتيب عشوائي كان بيحصل بسبب معرّفات الصفوف.
- شاشة "علاقات الموردين" لأي صنف ERP (كام مورد مرتبط بيه، باسمه وتكلفته عند كل مورد) — كانت مبنية من قبل بس مش ظاهرة في أي شاشة، دلوقتي بقت متاحة من شاشة الأصناف.
- فلتر "القسم" في شاشة قاعدة أصناف ERP.

## 1.2.0 — 2026-08-08

- حفظ تلقائي أثناء الكتابة (ملاحظات المرتجعة، كميات وتكلفة الأصناف، نماذج المورد والصنف) — أي تعديل يُحفظ فورًا في IndexedDB بدون زر حفظ، حتى لو أُغلق التطبيق فجأة.
- تصدير تقرير المرتجعة إلى Excel.
- تنزيل تقرير المرتجعة كصورة (PNG).
- إمكانية إخفاء أعمدة معينة (مثل التكلفة) قبل التصدير أو التنزيل كصورة.
- طباعة إيصال المرتجعة بتنسيق ملائم لماكينات الطباعة الحرارية (80mm).

## 1.1.0 — 2026-08-08

- رقم إصدار ظاهر في الشريط الجانبي.
- فحص تلقائي للتحديثات (كل 5 دقائق، وعند الرجوع للتبويب) مع إعادة تحميل تلقائية عند وجود نسخة أحدث منشورة.
- Service Worker بإستراتيجية "الشبكة أولًا" حتى تصل التحديثات فورًا عند الاتصال، مع نسخة احتياطية للعمل بدون إنترنت.
- سكريبت `scripts/bump-version.js` لرفع رقم الإصدار تلقائيًا في كل الملفات المعنية.
- GitHub Action يرفع رقم الإصدار تلقائيًا مع كل رفعة (push) على main.

## 1.0.0 — 2026-08-08

- الإصدار الأول: قاعدة أصناف ERP، الموردون، ربط أصناف المورد بالتكلفة وسجل تاريخي لها، المرتجعات بدورة حياة كاملة (مسودة/مُرسلة/مسجلة ERP/مؤرشفة)، معالج استيراد Excel، سجل عمليات، لوحة رئيسية، PWA قابل للتثبيت.
